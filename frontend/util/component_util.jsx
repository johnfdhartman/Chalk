//Keeping code dry by putting generic event handlers in here

// export const handleInput = (field,ctx) => (
//   (event) => {
//     this.setState({
//       [field]: event.target.value
//     });
//   }
// );
//
// //works when state is stored
// export const handleButtonClick = (buttonAction) => (
//   (event) => (data) =>(
//     buttonAction(data)
//   ))
// );
